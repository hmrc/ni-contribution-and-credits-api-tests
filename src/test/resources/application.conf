# Copyright 2024 HM Revenue & Customs
#

environment: local

local {
  services {
    host: "http://localhost"
    auth {
      port: 8585
      productionRoute: ""
    }
    ims {
      port: 9653
      productionRoute: ""
    }
  }
}

qa {
  # The actual values for desBearerToken and desEnvironment should be added only to private repos.
  # For public repos consider using an environment variable and provide the value at run time.
  #For example: In application.conf, set
  # desBearerToken: ${DES_BEARER_TOKEN}
  # and in the environment where the test is running `export DES_BEARER_TOKEN=<value>`.
  # See https://github.com/lightbend/config for more details.
  # Talk to PlatSec for other options.
  desBearerToken: "Bearer EvYRlYH8AS_hZGw32ffqJ25Mz04a"
  desEnvironment: "ist0"
  services {
    auth {
      host: "https://auth-login-api.protected.mdtp"
      productionRoute: ""
    }
    ims {
      host: "https://individuals-matching-api.protected.mdtp"
      productionRoute: ""
    }
    des {
      host: "https://des.ws.ibt.hmrc.gov.uk:443"
      productionRoute: "/enterprise"
    }
  }
}

staging {
  services {
    auth {
      host: "https://auth-login-api.protected.mdtp"
      productionRoute: ""
    }
    ims {
      host: "https://individuals-matching-api.protected.mdtp"
      productionRoute: ""
    }
  }
}
